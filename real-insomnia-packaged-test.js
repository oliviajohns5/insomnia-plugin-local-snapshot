'use strict';
const assert=require('assert');const fs=require('fs');const os=require('os');const path=require('path');const plugin=require('insomnia-plugin-local-snapshot');const fake='sk-'+'b'.repeat(30);const workspace=JSON.stringify({resources:[{_type:'request',name:'A',url:'https://x.test?api_key='+fake},{_type:'environment',name:'Env',data:{token:fake}}]});function ctx(out){const alerts=[];return{alerts,data:{export:{insomnia:async()=>workspace}},app:{showSaveDialog:async()=>out,getPath:async k=>k==='documents'?os.tmpdir():'',alert:async(t,m)=>alerts.push({t,m})}};}async function main(){assert(Array.isArray(plugin.workspaceActions));assert(Array.isArray(plugin.requestGroupActions));assert(Array.isArray(plugin.requestActions));assert(plugin.__test.makeSnapshot);const tmp=fs.mkdtempSync(path.join(os.tmpdir(),'local-snapshot-packaged-'));try{for(const action of [plugin.workspaceActions[0],plugin.requestGroupActions[0],plugin.requestActions[0]]){const out=path.join(tmp,Math.random().toString(36).slice(2)+'.md');await action.action(ctx(out));const text=fs.readFileSync(out,'utf8');assert(text.includes('Insomnia Local Snapshot'));assert(!text.includes(fake));}}finally{fs.rmSync(tmp,{recursive:true,force:true});}console.log('PASS: packaged plugin integration harness');}main().catch(e=>{console.error(e.stack||e);process.exit(1);});
